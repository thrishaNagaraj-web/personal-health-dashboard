services:
  - type: web
    name: personal-health-dashboard
    env: docker
    region: ohio
    plan: free
    disk:
      name: sqlite-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: RENDER_DISK_PATH
        value: /data
      - key: GROQ_API_KEY
        sync: false
