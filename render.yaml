services:
  - type: web
    name: personal-health-dashboard
    env: docker
    region: ohio
    plan: free
    disk:
      name: sqlite-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: RENDER_DISK_PATH
        value: /var/data
